version: '3.9'

services:
  proxy-scraper:
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
    working_dir: /app
    command: ["python", "run.py"]
    restart: "no"
      - "5000:5000"
    restart: unless-stopped
